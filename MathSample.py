import math

numA = 1.5
numB = -1.5

# floor関数
# 床(floor)は式の値以下の最大の整数を返す
print(math.floor(numA)) # 1
print(math.floor(numB)) # -2

# ceil関数
# 天井(celling)は式の値以上の最小の整数を返す
print(math.ceil(numA)) # 2
print(math.ceil(numB)) # -1